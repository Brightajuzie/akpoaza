import React from 'react';
import { View } from 'react-native';

const MapView = ({ children }) => <View>{children}</View>;
export const Marker = ({ children }) => <View>{children}</View>;
export const Polyline = () => null;
export default MapView;
